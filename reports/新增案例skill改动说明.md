# 新增故障案例并入 skill 的改动说明

来源：`cases/故障补充场景.json`（由 `故障补充场景.pptx` 提取，37 页，含 16 行概览表 + 31 条场景条目）。

> 2026-07-27 更新：按补充信息补齐了概览表里两个只有行、没有详情页的根因——**OSPF 两端 router-id 冲突**（PPT 中无对应页，`来源`字段标注为补充信息）和 **L2EVPN 二层环路**（补进原本只有标题的 p30）。概览表 16 行现已全部有对应详情页。

本次改动新增了一条增量并入流水线 `skill_case_merge_pipeline.py`：它先判断每个新案例是否已被既有 skill 覆盖，未覆盖的再决定是"在既有 skill 上追加小节"还是"新建 skill"，最后输出变更说明。

> 说明：已生成的 skill 库（`skills_distilled/<mm-dd>/`）和源文档树都不在仓库里，蒸馏用的模型接口也只在内网可达，所以本文给出的是**案例侧的处置方案**；在你的环境执行一次流水线后，会生成带实际改动内容的 `reports/skill_change_report_<mm-dd>.md`，那份才是逐条落地的最终结果。

## 一、流水线做了什么

| 阶段 | 动作 |
| --- | --- |
| 载入 | 读案例 JSON，剔除"仅有标题的补充页"和标注"已废弃 / 已从场景基线中去除"的条目，其余按`故障类型`归成案例组 |
| 定位 | 把既有 skill 清单（路径 + description + 小节标题）和案例摘要给模型，判定案例组应并入哪个既有 skill，或需要新建；新建时强制复用既有一级目录名 |
| 合并 | 同一目标 skill 的多个案例组合成一次调用，连同该 skill 全文交给模型，产出 `covered` / `append` / `create` 判定与内容；`overview_table`里同故障类型的行作为“诊断/修复目标”一并送入 |
| 应用 | 主进程串行写文件：`append` 追加小节到既有 skill，`create` 新建 skill 文件；`covered` 不动 |
| 报告 | 输出 `reports/skill_change_report_<mm-dd>.md`，含变更总览表、每处改动的完整内容、已覆盖清单、失败项、未并入条目 |

写入前有一道硬校验：追加内容不得带 frontmatter、必须含 `## ` 小节；新建内容必须以 frontmatter 开头。不合规就不落盘，只记进报告。变更说明写在 `reports/` 而不是 skill 目录，避免被 `validate_skills.py` 当成 skill 校验出 ERROR。

生成内容沿用主蒸馏流水线的写作约束：删除"联系技术支持/提交工程师"类步骤、跨分类引用写成 `[一级/二级.md]`、不照搬大段回显。另外针对这批案例补了一条：PPT 里的内部讨论批注（"—— 待确认…"、"请刘瑞…"、人名工号）不写进 skill。

## 二、需要并入的案例：20 条，14 个案例组

下表的"实际落点"来自 2026-07-27 在真实 skill 库（`skills_distilled/07-16`，59 篇）上跑的 DRY-RUN，全流程 0 失败（9 追加 + 3 新建 + 1 已覆盖），是实测结果而非推测。`修复方案`列标注该组案例是否给出了可执行的修复动作；仅能定位到根因的，skill 里只写到根因判定为止。

| # | 案例组（故障类型） | 根因（来源页） | 修复方案 | 实际落点 |
| --- | --- | --- | --- | --- |
| 1 | ISIS 邻居状态异常 | ISIS System ID 冲突（p5） | 有（NCE-IP 分配 System ID） | 追加 → `故障处理：IP路由/IS-IS故障案例.md` |
| 2 | OSPF 邻居状态异常 | 两端 IP 不在同一网段（p7）；两端 router-id 冲突（补充信息） | 部分（IP 可重分配；router-id 冲突缺诊断步骤，见 3.2） | 追加 → `故障处理：IP路由/OSPF故障案例.md` |
| 3 | BGP 邻居状态异常 | Peer IP / as-number / router-id 冲突（p9）；路由环路（p29） | 部分（Peer IP 与环路仅定位） | 追加 → `故障处理：IP路由/BGP故障案例.md` |
| 4 | BFD 状态异常 | 会话对端 IP 不匹配（p10）；对端标识符不匹配（p12） | 部分（标识符可自动修，IP 仅定位） | 追加 → `故障处理：网络可靠性/BFD故障案例.md` |
| 5 | LDP 隧道故障 | LDP 未使能（p13） | 有（全局 + 接口使能 mpls ldp） | 追加 → `故障处理：MPLS/MPLS LDP故障案例.md` |
| 6 | RSVP-TE 隧道故障 | 人工关闭隧道（p15） | 有（undo shutdown / 管控 API） | 追加 → `故障处理：MPLS/MPLS TE故障案例.md` |
| 7 | SR-TE 隧道故障 | 人工关闭隧道（p16） | 有（同上） | 追加 → `故障处理：Segment Routing/SR-MPLS TE故障案例.md` |
| 8 | SR-Policy 隧道故障 | 人工关闭 SR-Policy（p17） | 有（管控 API 置 admin-status up） | 追加 → 同上（与 SR-TE 合并为一次调用） |
| 9 | SRv6-Policy 隧道故障 | 人工关闭 SRv6-Policy（p19） | 有（同上） | **已覆盖，无需改动** → `故障处理：Segment Routing/SRv6 TE Policy故障案例.md` |
| 10 | PWE3 故障 | PW 远端 IP 不匹配（p20）；MTU 不一致（p21） | 有（远端地址取对端 LSR ID；MTU 对齐） | **新建** → `故障处理：VPN/PWE3故障案例.md` |
| 11 | VPLS 故障 | PW 远端 IP 不匹配（p22）；MTU 不一致（p23） | 有（同上，作用在 VSI peer） | 追加 → `故障处理：VPN/VPLS故障案例.md` |
| 12 | L2EVPN 故障 | 两端 Service ID 不匹配（p24）；二层环路（p30，补充信息） | 有（remote-service-id 取对端 local；环路按 LDT 告警决定是否 shutdown 子接口） | **新建** → `故障处理：VPN/L2EVPN故障案例.md` |
| 13 | 1588v2 时钟异常 | P/E 模式不匹配（p26） | 有（对齐上游 2~3 跳，缺省推 E2E） | 追加 → `故障处理：时间与时钟同步/1588v2故障案例.md` |
| 14 | 链路性能越限 | 切片链路带宽利用率过高（p31） | 有（切片链路带宽调整 API） | **新建** → `故障处理：IP业务/IP链路性能故障案例.md` |

定位阶段 14 个案例组全部成功：11 个并入既有 skill、3 个新建（PWE3、L2EVPN、链路性能三类既有库确实没有），合并阶段 13 个目标里 12 个产出改动、1 个判定已覆盖。BFD、Segment Routing、时间与时钟同步这几类既有 skill 都存在，直接追加。

两点需要复核：

1. **新建 skill 的命名不稳定。** 连跑两次，"链路性能越限"第一次落到 `故障处理：网络可靠性/切片链路性能越限故障案例.md`，第二次落到 `故障处理：IP业务/IP链路性能故障案例.md`——一级目录和文件名都变了。已把定位阶段的 temperature 从 0.4 降到 0（这是分类判断，不需要随机性），但新建路径仍建议人工确认后固定下来，否则重跑可能在两个位置各建一篇。并入既有 skill 的 11 个组两次结果完全一致，不受影响。
2. **SRv6-Policy 判了"已覆盖"，SR-TE / SR-Policy 判了"追加"。** 三者都是"人工关闭隧道"这一类根因，判定却不同。落盘前建议看一眼报告里 SRv6 那条的判定理由是否站得住——既有 `SRv6 TE Policy故障案例.md` 若确实已写到管理态 down 的排查与恢复，covered 是对的。

## 三、逐页盘点：37 页的去向

### 3.1 完全没有用到的 18 页

| 页码 | 标题/主题 | 未使用原因 | 影响 |
| --- | --- | --- | --- |
| p1–p4 | 未知 | 提取工具没产出任何内容，JSON 里没有这几页 | 推测是封面/目录/章节页，无影响；如含正文需重新提取 |
| p8 | 未知（夹在 p7 OSPF 与 p9 BGP 之间） | 同上，无内容 | 位置上可能是 OSPF 的配置举例页，建议核对 |
| p14 | 未知（夹在 p13 LDP 与 p15 RSVP-TE 之间） | 同上，无内容 | 可能是 LDP 的补充页，建议核对 |
| p32 | 未知（夹在 p31 性能与 p33 隧道之间） | 同上，无内容 | 建议核对 |
| p6 | ISIS：两端 IP 不在同一网段 | 标注**已废弃** —— 页内实验结论：IP 可以不在同一网段（含 p2p/p2mp），不会导致邻居 down | 无，结论可信 |
| p33 | 隧道故障：LSR ID 不匹配 | 标注**已从场景基线中去除** | 无 |
| p34 | SR-TE：LSR ID 不匹配 | 同上 | 无 |
| p35 | SR-TE：SID 冲突 | 同上 | ⚠️ 与概览表冲突，见 3.3 |
| p36 | SR-Policy：SID 冲突 | 同上 | ⚠️ 同上 |
| p37 | SRv6-Policy：SID 冲突 | 同上 | ⚠️ 同上 |
| p11 | 物理线路错连，影响 OSPF 场景 | 只有标题，无诊断/方案正文 | **真缺口** —— 独立场景，其它页没覆盖 |
| p18 | SR-Policy：人工关闭 SR Policy | 只有标题 | 无 —— p17 已完整覆盖同主题同根因 |
| p25 | L2EVPN：两端 Service ID 不匹配 | 只有标题 | 无 —— p24 已完整覆盖 |
| p27 | BGP Peer Down：ISIS 路由环路 | 只有标题 | **真缺口** —— p29 的定位步骤停在“待定”，缺的正是这里 |
| p28 | 案例：ISIS 路由环路 | 只有标题 | **真缺口** —— 同上 |

脚本按`已废弃`/`已从场景基线中去除`标注和“仅有标题”自动跳过后 11 条；p1–p4、p8、p14、p32 是提取阶段就没有内容，不在案例 JSON 里。p30 原本也在这张表里，现已由补充信息补全，不再跳过。

### 3.2 已使用但内容存疑的 16 页

| 页码 | 存疑点 | 类型 | 建议处置 |
| --- | --- | --- | --- |
| p5 | ①“老版本设备查询命令会否有差异”未结论；②“是否要生成 commit，能否在下发接口里加 commit”未定 | 遗留问题 | commit 影响生成的配置能否直接下发，需拍板 |
| p7 | ①唯一性依存量判断，“少量设备未纳管或同步不及时会不准”；②两端都报告警时可能左右各改一次；③“如何避免把符合规划意图的一端改掉” | 方案风险未闭环 | 自动改 IP 有误改风险，建议先按只诊断落地 |
| p9 | 修复依赖“BGP router-id 资源池”，页内自述**当前无此资源池**，只有“取 lsr-id”的待验证思路 | 依赖不存在 | router-id 冲突的自动修复暂不可落地 |
| p10 | “两端存在多个 BFD 会话时如何找到对端 session”标注待确认 | 定位逻辑不完整 | 与 p12 的标识符匹配法合并可解，需确认 |
| p13 | ①修复只支持单链路（多链路无法判断哪条该使能）；②远端 LDP 场景“实验室未构造出，待确认” | 约束/未验证 | 约束需写进 skill，远端场景暂不覆盖 |
| p16 | 根因判据写着“若存在 shutdown 则根因为**人工关闭 RSVP-TE 隧道**” | 复制粘贴错误 | 应为 SR-TE，需改源数据 |
| p17 | “拿不到正确的管理状态？怎么不和 RSVP 一样走命令行查看回显？” | 诊断手段存疑 | 走管控接口还是命令行未定 |
| p19 | ①故障类型写成“**Rv6**-Policy隧道故障”（漏 S）；②同 p17 的管理状态问题 | 拼写错误 + 同上 | 拼写错会导致独立成组、误判为新建 skill |
| p20 | ①备注写“可以诊断，**无法修复**，要修复依赖全局 lsr-id 历史信息”，与`方案生成`给出的自动修复矛盾；②“建议改成 MTU 不一致，请刘瑞新增一页” | 结论自相矛盾 | 需确认远端 IP 不匹配到底能不能自动修 |
| p21 | `根因类型`仍写“PW 远端 IP 地址不匹配”，正文讲的是 **MTU 不一致**；`故障诊断`只有“已支持”三个字 | 元数据与正文不符 | 改根因类型为 MTU 不一致，补诊断步骤 |
| p22 | 备注写“可以诊断，**无法定位**”，与正文给出的定位步骤矛盾 | 结论自相矛盾 | 同 p20 |
| p23 | ①同 p21 的根因类型错标；②`故障诊断`只有“已支持”；③“是否存在 1 对多 MTU 不一致的情况”未确认 | 元数据不符 + 待确认 | 同 p21 |
| p26 | `方案生成`字段只有“1、通过”——PPT 里被截断；真正的修复思路（往上找 2~3 跳对齐、不统一时优先 E2E、三方设备不支持）散落在`备注` | 内容截断 | 建议在源数据里把方案补全 |
| p29 | “定位导致环路的配置：**待定**”，并写明需要路由策略/cost 规划分析、“建议采用仿真验证技术” | 方案未完成 | 只能落地到“发现环路 + 给出环路位置”为止 |
| 补充信息（OSPF router-id 冲突） | `故障诊断`只有一句“需要补充：ospf router id 的不同配置方式，进程之间的 id 冲突，考虑多进程的情况”，是待办而非步骤；`方案生成`为空，修复思路（按 loopback 最大地址分配 router-id）填在`对管控依赖`里 | 诊断缺失 | 按现状生成的小节只会写到根因判定；补上诊断步骤后重跑，或把分配规则挪到`方案生成` |
| p31 | ①“默认切片是否要监控”未定；②“切片口不配 IP，IGP link 如何对应到切片链路”未解；③“是否有不存在切片 id 的切片口”未定；④“QoS 限速带宽与物理接口带宽不一致是否支持”未定；⑤“默认切片需保留的最低带宽”未定 | 5 处待确认 | 待确认项最多的一页，落地前需集中澄清 |

无问题、可直接用的只有 4 条：**p12**（BFD 标识符不匹配）、**p15**（RSVP-TE 人工关闭）、**p24**（L2EVPN Service ID 不匹配）、**p30**（二层环路，补充信息补全）。

上表中的内部讨论批注（“—— 待确认…”、“请刘瑞…”、人名工号）已在提示词里明确要求不写进 skill，但**元数据错误（p16/p19/p21/p23）和内容截断（p26）会直接影响分组与生成结果，建议先改源数据再跑**。

### 3.3 跨页矛盾

| 矛盾点 | 涉及位置 | 说明 |
| --- | --- | --- |
| SID 冲突到底算不算基线场景 | 概览表 vs p35/p36/p37 | 概览表把“SID 冲突”列为 SR-TE / SR-Policy / SRv6-Policy 的诊断项，但这 3 页详情都标“已从场景基线中去除”。目前脚本按详情页跳过 |
| 人工关闭类隧道的诊断字段写反 | 概览表 vs p15/p16/p17 | 概览表里“人工关闭 RSVP-TE 隧道”的诊断写成“隧道目的地址与对端 LSR ID 不匹配”、“人工关闭 SR-TE”写成“SID 冲突”——是 p33~p37 那批已下线内容串行到了概览表 |
| ISIS 路由环路的归属 | 概览表 vs p29 | 概览表把它归在“BGP 邻居状态异常”下，p27/p28 的标题也是 BGP Peer Down，但根因是 ISIS 层面的环路，写进 BGP skill 还是 ISIS skill 需要定 |

### 3.4 `overview_table` 与详情页的对应关系

概览表 16 行、有正文的详情页 26 条，设计上一一对应。补齐前有 5 处对不上，补齐后只剩概览表漏行的 3 条。流水线按`故障定义`把概览行挂到对应的案例组上，作为“诊断目标 / 修复目标”随案例详情一起送给模型；`audit_overview_coverage()` 会在运行开始时把下面两类偏差打成 WARN。

**只有概览行、没有可用详情页：已清零。** 补齐前是这 2 行：

| 故障定义 | 故障根因 | 补齐方式 |
| --- | --- | --- |
| OSPF 邻居状态异常 | OSPF Router ID 冲突 | 按补充信息新增一条案例，`slide`为 null、`来源`标注“补充信息，PPT 中无对应页” |
| L2EVPN 故障 | 二层环路 | 按补充信息补全 p30（原本只有标题） |

**有详情页、概览表没列（3 条）** —— 概览表疑似漏行，不影响并入：

| 页码 | 故障类型 / 根因 |
| --- | --- |
| p10 | BFD 状态异常 / BFD 会话对端 IP 不匹配（概览表只列了“对端标识符不匹配”） |
| p26 | 1588v2 时钟异常 / P/E 模式不匹配 |
| p31 | 链路性能越限 / 切片链路带宽利用率过高 |

**概览行的`诊断`列有 4 行是错的**：`人工关闭`类的 4 行——RSVP-TE（写成“隧道目的地址与对端 LSR ID 不匹配”）、SR-TE（“SID 冲突等”）、SR-Policy 和 SRv6-Policy（“SID 冲突，要求 SID 在 IGP 域内唯一”）——描述的都是 p33~p37 那批已下线场景的判据，和`故障根因`列的“人工关闭”对不上。这些文字会随概览行进入提示词，因此在案例详情的抬头加了一句：概览块只是纲要，与“案例”块冲突时以案例块为准。要根治仍需修概览表原文。

## 四、仍需补充原始素材的 4 处

p30 二层环路已由补充信息补全，不再列入；OSPF router-id 冲突补进来了，但诊断步骤仍是待办，所以总数还是 4。

| 主题 | 来源页 | 现状 |
| --- | --- | --- |
| 物理线路错连影响 OSPF | p11 | 只有标题；p7 的“两端 IP 不同网段”是另一回事，不能替代 |
| ISIS 路由环路的具体案例 | p27、p28 | 只有标题；p29 给了 ping + tracert 的定位方法，但“定位导致环路的配置”待定 |
| 1588v2 时钟修复方案 | p26 | `方案生成`被截断成“1、通过”，思路只在备注里 |
| OSPF router-id 冲突的诊断步骤 | 补充信息 | 补充信息只写了“需要补充：…多进程情况”，缺实际判据 |

## 五、怎么跑

```bash
python3 skill_case_merge_pipeline.py
python3 validate_skills.py skills_distilled/$(date +%m-%d)
```

默认参数在 `skill_case_merge_pipeline.py` 文件末尾的 `main()` 调用里改：`CASES_PATH` 换案例文件，`SKILL_DIR` 指向要并入的 skill 目录，`DRY_RUN=True` 只出报告不落盘（建议先跑一次 dry-run 核对定位结果）。有处理失败项时退出码为 1，失败原因写在报告的"定位阶段失败的案例组"和总览表里，改完重跑即可——`append` 会重复追加，重跑前记得用 git 回滚 skill 目录。
