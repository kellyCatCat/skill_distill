# 复制为 .env 后填入真实地址与密钥（.env 不入库）：cp .env.example .env

# qwen3.8-27b / qwen3.6-27b（同一个部署，共用这组变量）
QWEN_BASE_URL=http://<host>:<port>/v1
QWEN_API_KEY=

# 输出预算，留空用 model_config.py 里的默认值
# QWEN_MAX_TOKENS=16384
